apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "syslogserver.fullname" . }}-test-connection"
  labels:
    {{- include "syslogserver.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "syslogserver.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
